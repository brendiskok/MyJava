package by.sveta.les01.main;

public class Nine {

	public static void main(String[] args) {
		// TODO (a / c) * (b / d) - (a * b - c) / c * d
		
		double a = 1.0;
		double b = 1.0;
		double c = 1.0;
		double d = 1.0;
		
		double res = (a / c) * (b / d) - (a * b - c) / c * d;
		
		System.out.println(res);

	}

}
